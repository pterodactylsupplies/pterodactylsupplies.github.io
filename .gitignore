# Old page versions and experiments — kept locally, not published.
_archive/

# Claude Code's local config: dev-server definitions and per-machine
# permission settings. Nothing here belongs on a public website.
.claude/

# Local font experiments — the .otf files are licensed desktop fonts
# and must not be published on a public site.
font-lab/
